# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git運用ルール

コードを変更するたびに、以下の手順でGitHubにプッシュすること：

```bash
git add .
git commit -m "変更内容を説明するメッセージ"
git push origin main
```

- コミットメッセージは日本語でも英語でも可。変更内容が明確に伝わるように書くこと。
- リモートリポジトリ: https://github.com/kasa555302/Delivery_Slip_Manager.git
- デフォルトブランチ: `main`
